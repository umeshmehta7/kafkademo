name: test workflow
on: [push, pull_request]
jobs:
  my-test-command:
    runs-on: ubuntu-latest
    steps:
     - name: umesh
     - run: pwd
  run-shell-command:
    runs-on:  ubuntu-latest
    steps:
      - name: print message
        run: echo "Welcome to github action"

      - name: multi line command
        run: |
          pwd
          ls